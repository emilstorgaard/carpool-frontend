services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: carpool-frontend
    restart: unless-stopped
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
